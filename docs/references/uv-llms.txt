Reference notes for Python environment and dependency workflow.

Current direction:
- prefer a reproducible Python workflow
- keep dependencies explicit
- update this file if the repo standardizes on `uv` commands and lockfile policy
